import React from 'react';
import { Scene, Router, Text } from 'react-native-router-flux';
import LoginForm from './components/LoginForm';

const RouterComponent = () => {
  return(
    <Text/>
  );
};

export default RouterComponent;